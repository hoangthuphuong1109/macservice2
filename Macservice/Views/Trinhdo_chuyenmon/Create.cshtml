@model Macservice.Models.Trinhdo_chuyenmon

@{
    ViewBag.Title = "Thêm mới trình độ chuyên môn";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<ol class="breadcrumb">
    <li><a href="/Trinhdo_chuyenmon">Quản lý trình độ chuyên môn </a></li>
    <li class="active">@ViewBag.Title</li>
</ol>

<h2>Thêm mới trình độ chuyên môn</h2>
<div class="row">
    <div class="col-sm-6">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            <div class="form-group">
                <label>Mã trình độ chuyên môn<span style="color:red">*</span></label>
                <input type="text" readonly="readonly" value='Tự động tạo mã' class="form-control" />
            </div>
            <div class="form-group">
                <label>Tên trình độ chuyên môn<span style="color:red">*</span></label>
                @Html.EditorFor(model => model.Tentrinhdochuyenmon, new { htmlAttributes = new { @class = "form-control", @required = "@required" } })
                @Html.ValidationMessageFor(model => model.Tentrinhdochuyenmon, "", new { @class = "text-danger" })
            </div>

            <hr />
            <div class="form-group">
                <input type="submit" value="Thêm mới" class="btn btn-success" />
                @Html.ActionLink("Quay lại danh sách", "Index", null, new { @class = "btn btn-default" })

            </div>
        }
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
